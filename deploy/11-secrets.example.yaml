# EXAMPLE ONLY — do not commit real secrets. In production this Secret is
# synced from OCI Vault (e.g. via the OCI Secrets Store CSI driver or
# External Secrets Operator), not applied by hand.
#
# The values below are placeholders showing the required keys.
apiVersion: v1
kind: Secret
metadata:
  name: pdf2html-secrets
  namespace: pdf2html
type: Opaque
stringData:
  REDIS_URL: "redis://:CHANGE_ME@<oci-cache-host>:6379/0"
  JSONDB_URL: "CHANGE_ME_autonomous_json_db_connection_string"
  QUEUE_OCID: "ocid1.queue.oc1..CHANGE_ME"
  QUEUE_ENDPOINT: "https://cell-1.queue.messaging.<region>.oci.oraclecloud.com"
  OCI_NAMESPACE: "bmzbbujw9kal"
  # CMS export (optional)
  STRAPI_API_TOKEN: "CHANGE_ME"
